name: Release Builds

on:
  workflow_dispatch:
  release:
    types: [published]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-arm64-publish:
    runs-on: macos-latest

    env:
      ARTIFACT_NAME: cutler-${{ github.event.release.name }}-darwin-arm64

    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cache Cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build release artifact
        run: cargo build --release --verbose --locked

      - name: Package release
        run: |
          mkdir -p ${{ env.ARTIFACT_NAME }}/bin ${{ env.ARTIFACT_NAME }}/man/man1
          cp target/release/cutler ${{ env.ARTIFACT_NAME }}/bin/
          cp man/man1/cutler.1 ${{ env.ARTIFACT_NAME }}/man/man1/
          cp LICENSE ${{ env.ARTIFACT_NAME }}/
          cp README.md ${{ env.ARTIFACT_NAME }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}
          compression-level: 9
          if-no-files-found: error
          retention-days: 90

      - name: Prepare Crates.io README
        run: sed -i '' '/^>/d' README.md

      - name: Publish to crates.io
        env:
          CRATES_TOKEN: ${{ secrets.CRATES_TOKEN }}
        run: cargo publish --token $CRATES_TOKEN --allow-dirty

  build-x86_64:
    runs-on: macos-13

    env:
      ARTIFACT_NAME: cutler-${{ github.event.release.name }}-darwin-x86_64

    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cache Cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build release artifact
        run: cargo build --release --verbose --locked

      - name: Package release
        run: |
          mkdir -p ${{ env.ARTIFACT_NAME }}/bin ${{ env.ARTIFACT_NAME }}/man/man1
          cp target/release/cutler ${{ env.ARTIFACT_NAME }}/bin/
          cp man/man1/cutler.1 ${{ env.ARTIFACT_NAME }}/man/man1/
          cp LICENSE ${{ env.ARTIFACT_NAME }}/
          cp README.md ${{ env.ARTIFACT_NAME }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}
          compression-level: 9
          if-no-files-found: error
          retention-days: 90
